# Installation

```bash
npm install simplejsnode
```

The package ships its own TypeScript types — no separate `@types` package is required.

**Requirements:** Node.js `>=20`.

Once installed, head to the [Quick Start](/guide/quick-start) to create your first server.
